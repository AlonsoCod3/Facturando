export const environment = {
    production: false,
    // apiUrl: process.env['API_URL'], // Se leerá de la variable de entorno de Render
    // apiUrl: import.meta.env.NG_APP_URL, // Se leerá de la variable de entorno de Render
  };